# py-clob-client       # Optional: only needed for LIVE order placement (requires Python <=3.12)
python-dotenv
websockets
loguru
httpx                   # Polymarket REST API (market discovery + real prices)
